/**
 * 자바스크립트 코드의 구문 패턴을 살펴보면 표현식(Expressions)과 명령문(Statements), 두 종류로 나눌 수 있다.
   *  표현식(Expressions)은  값을 생성한다. 연산자를 통해 값을 생성하거나, 변수 또는 함수 인자로 값을 넣을 때 표현식을 사용한다.
   *  명령문(Statements)은 일종의 헹동 또는 행위를 수행하게 하는 코드이다. 자바스크립트의 명령문은 if, if-else, for, switch 등이 있다.
 */

/*
(3 + 12) / 5;
declaredVariable;
greeting('Hello');

if (true) {

}

function greeting() {
    'hello'
    'Chloe' + 3
    greeting();
}
 */

// greeting(if(ture) { console.log('It is not acceptable') }); // 잘못된 문장으로 구문 오류가 발생
